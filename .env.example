DATABASE_URL=postgresql://sendro:sendro@localhost:5432/sendro
API_URL=http://localhost:3001
DASHBOARD_URL=http://localhost:3000
BETTER_AUTH_SECRET=replace-me
STRIPE_API_KEY=replace-me
# Local verification flow for S01/T02:
# 1. docker compose up -d postgres
# 2. export DATABASE_URL from this file (or load it via your shell)
# 3. pnpm --filter @repo/db db:generate
# 4. pnpm --filter @repo/db db:migrate
