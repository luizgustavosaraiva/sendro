DATABASE_URL=postgresql://sendro:sendro@localhost:5432/sendro
API_URL=http://localhost:3001
DASHBOARD_URL=http://localhost:3000
BETTER_AUTH_SECRET=0123456789abcdef0123456789abcdef
STRIPE_API_KEY=sk_test_sendro_local
EVOLUTION_API_URL=http://localhost:8080
EVOLUTION_API_KEY=sendro-evolution-local-key
# Optional if you want real LLM extraction for WhatsApp intake (otherwise stub extractor is used)
# OPENAI_API_KEY=
# LLM_BASE_URL=
# LLM_MODEL=
# Local verification flow for full stack + Evolution Go:
# 1. docker compose up -d postgres evolution
# 2. pnpm --filter @repo/db db:migrate
# 3. docker compose up -d api dashboard
# 4. Open Evolution Manager at http://localhost:8080/manager/login and activate license
# 5. In dashboard: /dashboard/whatsapp -> Connect to generate QR
# 6. Scan QR using your WhatsApp app
